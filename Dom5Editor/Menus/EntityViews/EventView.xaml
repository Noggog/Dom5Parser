<UserControl x:Class="Dom5Editor.EventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dom5Editor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Background="{StaticResource BackgroundBrush}" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with Name Display -->
            <GroupBox Header="Event Information" Grid.Row="0" Style="{StaticResource CustomGroupBoxStyle}">
                <ContentControl Content="{Binding Name}"/>
            </GroupBox>

            <!-- Description -->
            <GroupBox Header="Description" Grid.Row="1" Style="{StaticResource CustomGroupBoxStyle}">
                <TextBox Text="{Binding Description.Value}" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" MinHeight="100" Style="{StaticResource ModernTextBoxStyle}"/>
            </GroupBox>

            <!-- Properties List with Remove Option -->
            <GroupBox Header="Properties" Grid.Row="2" Style="{StaticResource CustomGroupBoxStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding EntityProperties}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentControl Grid.Column="0" Content="{Binding}"/>
                                    <Button Grid.Column="1" Content="X" 
                                            Command="{Binding DataContext.RemovePropertyCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                            CommandParameter="{Binding}" 
                                            Style="{StaticResource RemoveButtonStyle}"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </GroupBox>

            <!-- Add Property Controls -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" ItemsSource="{Binding AvailableCommands}" SelectedItem="{Binding SelectedCommand}" Margin="2"/>
                <Button Grid.Column="1" Content="Add Property" Command="{Binding AddPropertyCommand}" Margin="2" Style="{StaticResource ModernButtonStyle}" Padding="5,2"/>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>