<UserControl x:Class="Dom5Editor.SiteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dom5Editor" xmlns:menus="clr-namespace:Dom5Editor.Menus"
            xmlns:commands="clr-namespace:Dom5Edit.Commands;assembly=Dom5Edit"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="CommandEnumProvider" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="commands:Command"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid x:Name="SiteMenuHolder" Margin="0" >
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="200*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="0">
            <!-- Name box -->
            <ContentControl Margin="2" HorizontalAlignment="Left"  Content="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged}" MinWidth="360"/>
        </Grid>
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Grid.RowSpan="2">
            <Grid Margin="0,0,546,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Column="0" Grid.RowSpan="3">
                    <ContentControl>
                        <ItemsControl Background="Transparent" ItemsSource="{Binding Path=SitePropertiesView}" IsTextSearchEnabled="False"/>
                    </ContentControl>
                    <ComboBox ItemsSource="{Binding Path=SiteCommandsView}"
                        SelectedItem="{Binding SelectedCommand}" />
                    <Button Content="Add Property" Command="{Binding AddPropertyCommand}" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
